{
  "name": "sunset-circuit",
  "version": "1.0.0",
  "description": "A pseudo-3D arcade racer that runs in the browser with no build step.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "node --test test/*.test.js",
    "start": "python3 -m http.server 8080"
  }
}
